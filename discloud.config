TYPE=bot
MAIN=bot/index.js
AUTORESTART=true
# Limit V8 old space to ~100 MB. This affects JS heap, not total process memory.
# Discloud may still enforce platform-level limits via the dashboard.
START=node --max-old-space-size=100 bot/index.js
BUILD=npm install
NODE_VERSION=18
MEMORY=100M
VLAN=true